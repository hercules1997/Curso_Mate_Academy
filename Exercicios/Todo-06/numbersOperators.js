/*
        Existem as variáveis a e b em nosso código. Sua tarefa é declarar 4 novas variáveis:

        addition deve conter a soma de a e b;
        subtraction deve conter a diferença entre a e b;
        division deve conter o resultado da divisão de a por b;
        multiplication deve conter o resultado da multiplicação de a por b.
*/

const a = 10;
const b = 2;

// write your code below this line

const addition = a + b;
const subtraction = a - b;
const division = a / b;
const multiplication = a * b;